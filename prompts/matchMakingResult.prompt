---
model: googleai/gemini-1.5-flash-latest
input:
  schema:
    userID: string
    data: string
output:
  format: json
  schema:
    data: string
---

My user ID is ${userID} and I am an user in the following data. Suggest 3 people from the following data ${data}that
share the most similarity with me. Please format the response as a JSON file